import { memo } from 'react';

const DashboardPage = () => {
  return (
    <div className="flex items-center justify-center h-full min-h-[400px] text-text-subtle">
      Dashboard coming soon
    </div>
  );
};

export default memo(DashboardPage);
